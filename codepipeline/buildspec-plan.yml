version: 0.2

phases:
  install:
    commands:
      - terraform version

  pre_build:
    commands:
      - terraform init

  build:
    commands:
      - terraform plan -out=tfplan

artifacts:
  files:
    - tfplan